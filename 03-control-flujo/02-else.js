let edad = 9; // Declaramos una variable 'edad' con un valor de 15

// Verificamos la edad del usuario
if (edad > 17) { // Si la edad es mayor a 17, el usuario es mayor de edad
    console.log('Usuario mayor de edad'); // Mostramos un mensaje en la consola
} else if (edad > 13) { // Si la edad es mayor a 13 pero menor o igual a 17
    console.log('Usuario ingresa acompañado de un adulto'); // Mensaje indicando que requiere supervisión
} else { // Si la edad es 13 o menor
    console.log('Usuario no ingresa'); // Mensaje indicando que es menor de edad
}


